
@model IEnumerable<PropertyManagement.BusinessObjectLayer.ViewModels.PropertiesForLeaseViewModel>

<div class="row">
    <div class="col-md-12">
        <div class="box box-info " id="nopcommerce-news-box">
            <div class="box-body">
                @Html.Partial("Partials/_LeaseMonths")

            </div>
            <hr />
            <div class="box-header with-border clearfix">
                <div class="box-title">
                    <i class="fa fa-newspaper-o"></i>
                    Leases for: Year - Month
                </div>
            </div>
            <div class="box-body">
                <table class="table table-striped table-bordered" id="TblProperty">
                    <thead>
                        <tr>
                            <th>Property</th>
                            <th>Address</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PropertyId)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PropertyAddress)
                                </td>
                                <td>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</div>
